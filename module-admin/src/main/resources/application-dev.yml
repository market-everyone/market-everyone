# dev 환경
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/testdb"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update